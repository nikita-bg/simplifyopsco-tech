{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ai-voice-shopping-assistant && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd ai-voice-shopping-assistant && npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
