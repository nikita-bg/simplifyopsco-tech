---
phase: 06-agent-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent_config_service.py
  - backend/models.py
  - backend/main.py
  - backend/tests/test_agent_config.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, CFG-05, CFG-06, CFG-07]

must_haves:
  truths:
    - "GET /api/agent/config/{store_id} returns full agent config (voice, greeting, widget, personality, language)"
    - "PUT /api/agent/config/{store_id} saves config to DB and pushes to ElevenLabs"
    - "GET /api/voices returns curated voice list with preview URLs"
    - "GET /api/agent/embed-code/{store_id} returns copy-paste embed snippet"
    - "Personality presets return system_prompt templates"
    - "Enable/disable toggle updates agent_status and settings.enabled"
  artifacts:
    - path: "backend/agent_config_service.py"
      provides: "Personality presets, curated voice list, config merge logic"
      min_lines: 60
    - path: "backend/tests/test_agent_config.py"
      provides: "Tests for all config endpoints"
      min_lines: 80
    - path: "backend/main.py"
      provides: "4 new endpoints: GET/PUT config, GET voices, GET embed-code"
      contains: "api/agent/config"
    - path: "backend/models.py"
      provides: "AgentConfigResponse, AgentConfigUpdate, VoiceOption, EmbedCodeResponse"
      contains: "AgentConfigResponse"
  key_links:
    - from: "backend/main.py (PUT /api/agent/config)"
      to: "elevenlabs_service.update_agent"
      via: "httpx call to ElevenLabs API"
      pattern: "elevenlabs_service\\.update_agent"
    - from: "backend/main.py (PUT /api/agent/config)"
      to: "stores table (settings JSONB + agent_config JSONB)"
      via: "DB update after ElevenLabs success"
      pattern: "UPDATE stores SET"
    - from: "backend/main.py (GET /api/voices)"
      to: "agent_config_service.get_curated_voices"
      via: "service call"
      pattern: "get_curated_voices"
---

<objective>
Backend API endpoints for agent configuration: full config CRUD, curated voice list with previews, personality presets, embed code generation, and enable/disable toggle.

Purpose: Provides the data contract and backend logic that the frontend config page (Plan 02) will consume. All ElevenLabs integration, config merge logic, and validation lives here.

Output: 4 new API endpoints, Pydantic models, personality presets module, and comprehensive tests.
</objective>

<execution_context>
@C:/Users/Nikita/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nikita/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/main.py
@backend/models.py
@backend/elevenlabs_service.py
@backend/config.py

<interfaces>
<!-- Existing contracts the executor needs -->

From backend/models.py:
```python
class AgentUpdateRequest(BaseModel):
    voice_id: Optional[str] = None
    greeting: Optional[str] = None
    language: Optional[str] = None
    system_prompt: Optional[str] = None
    max_duration_seconds: Optional[int] = None

class WidgetConfigResponse(BaseModel):
    has_agent: bool = False
    enabled: bool = False
    agent_id: Optional[str] = None
    widget_color: str = "#256af4"
    widget_position: str = "bottom-right"
    greeting_message: Optional[str] = None
    status: str = "unknown"

class StoreSettings(BaseModel):
    widget_color: str = "#6366f1"
    widget_position: str = "bottom-right"
    voice_id: Optional[str] = None
    greeting_message: str = "Hi! I can help you..."
    language: str = "en"
    enabled: bool = True
```

From backend/elevenlabs_service.py:
```python
class ElevenLabsService:
    async def update_agent(self, agent_id, conversation_config=None, platform_settings=None, name=None, tools=None) -> dict
    async def get_agent(self, agent_id: str) -> dict
```

From backend/main.py:
```python
# Existing PATCH /api/agents/{store_id} handles voice_id, greeting, language, system_prompt
# Existing GET /api/widget/config reads from stores.settings JSONB
# require_store_owner(request, store_id) for auth
# _parse_jsonb(value) helper for JSONB parsing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agent config service module + Pydantic models + test stubs</name>
  <files>backend/agent_config_service.py, backend/models.py, backend/tests/test_agent_config.py</files>
  <behavior>
    - Test: get_curated_voices() returns list of 10 VoiceOption objects with id, name, preview_url, gender, accent
    - Test: get_personality_presets() returns 6 presets each with id, name, description, system_prompt
    - Test: "friendly" preset system_prompt contains merchant-appropriate tone instructions
    - Test: AgentConfigResponse model validates with all fields (voice_id, greeting, widget_color, widget_position, enabled, language, personality_preset)
    - Test: AgentConfigUpdate model accepts partial updates (all fields optional)
    - Test: generate_embed_code(store_id, api_url) returns valid HTML script tag with correct data attributes
  </behavior>
  <action>
    1. Create `backend/agent_config_service.py` with:
       - `CURATED_VOICES`: list of 10 dicts — curate from ElevenLabs popular voices. Each has: id (ElevenLabs voice ID), name, preview_url (https://api.elevenlabs.io/v1/voices/{id}/preview), gender, accent, description. Include mix of male/female, American/British/other accents.
       - `PERSONALITY_PRESETS`: list of 6 dicts — Friendly (default, warm helpful tone), Professional (formal business), Energetic (upbeat enthusiastic), Calm (soothing patient), Expert (knowledgeable authoritative), Concise (brief efficient). Each has: id (slug), name, description (1 sentence), system_prompt (full prompt template with {store_name} placeholder).
       - `SUPPORTED_LANGUAGES`: list of 28+ dicts with code and name (en, es, fr, de, it, pt, nl, pl, sv, da, no, fi, ro, hu, cs, el, bg, tr, ar, hi, ja, ko, zh, th, vi, id, ms, uk).
       - `get_curated_voices()` -> list of VoiceOption
       - `get_personality_presets()` -> list of PersonalityPreset
       - `get_supported_languages()` -> list of LanguageOption
       - `generate_embed_code(store_id: str, api_url: str)` -> str (returns script tag HTML)

    2. Add to `backend/models.py`:
       - `VoiceOption(BaseModel)`: id, name, preview_url, gender, accent, description
       - `PersonalityPreset(BaseModel)`: id, name, description, system_prompt
       - `LanguageOption(BaseModel)`: code, name
       - `AgentConfigResponse(BaseModel)`: voice_id (Optional[str]), voice_name (Optional[str]), greeting (str), widget_color (str), widget_position (str), enabled (bool), language (str), personality_preset (Optional[str]), agent_status (str)
       - `AgentConfigUpdate(BaseModel)`: voice_id (Optional[str]), greeting (Optional[str]), widget_color (Optional[str]), widget_position (Optional[str]), enabled (Optional[bool]), language (Optional[str]), personality_preset (Optional[str])
       - `EmbedCodeResponse(BaseModel)`: embed_code (str), store_id (str)

    3. Create `backend/tests/test_agent_config.py` with test classes for the service module. Use standard pytest patterns from existing tests. No DB/API mocking needed for service tests — they test pure functions.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_agent_config.py -x -q</automated>
  </verify>
  <done>
    - agent_config_service.py exports curated voices (10), personality presets (6), languages (28+), embed code generator
    - All new Pydantic models validate correctly
    - All service unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four API endpoints with full integration</name>
  <files>backend/main.py, backend/tests/test_agent_config.py</files>
  <behavior>
    - Test: GET /api/agent/config/{store_id} returns 200 with voice, greeting, widget, language, personality fields from DB
    - Test: GET /api/agent/config/{store_id} returns 404 for unknown store
    - Test: PUT /api/agent/config/{store_id} with {voice_id: "abc"} updates ElevenLabs then DB, returns updated config
    - Test: PUT /api/agent/config/{store_id} with {enabled: false} sets agent_status='inactive' and settings.enabled=false
    - Test: PUT /api/agent/config/{store_id} with {enabled: true} sets agent_status='active' and settings.enabled=true
    - Test: PUT /api/agent/config/{store_id} with {personality_preset: "professional"} looks up preset system_prompt and pushes to ElevenLabs
    - Test: PUT /api/agent/config/{store_id} with {widget_color: "#ff0000"} updates settings JSONB only (no ElevenLabs call for widget-only changes)
    - Test: GET /api/voices returns list of curated voices
    - Test: GET /api/agent/embed-code/{store_id} returns embed HTML with correct store_id and api_url
  </behavior>
  <action>
    Add 4 endpoints to `backend/main.py`:

    1. **GET /api/agent/config/{store_id}** (auth: require_store_owner):
       - Query: `SELECT elevenlabs_agent_id, agent_status, agent_config, settings FROM stores WHERE id = $1::uuid`
       - Parse settings JSONB (use existing _parse_jsonb pattern)
       - Look up voice_name from CURATED_VOICES by voice_id
       - Return AgentConfigResponse with fields merged from agent_config and settings

    2. **PUT /api/agent/config/{store_id}** (auth: require_store_owner, body: AgentConfigUpdate):
       - Separate fields into ElevenLabs-bound (voice_id, greeting, language, personality_preset) and DB-only (widget_color, widget_position, enabled)
       - If personality_preset provided: look up system_prompt from PERSONALITY_PRESETS, add to ElevenLabs payload
       - If any ElevenLabs fields changed: build conversation_config dict (same pattern as existing PATCH /api/agents/{store_id}), call elevenlabs_service.update_agent FIRST, then DB
       - If enabled changed: UPDATE stores SET agent_status = 'active'/'inactive', also update settings JSONB enabled field
       - Always: UPDATE stores SET settings = settings || {widget changes}::jsonb, updated_at = NOW()
       - Return updated AgentConfigResponse

    3. **GET /api/voices** (no auth required):
       - Return agent_config_service.get_curated_voices()
       - Also return get_supported_languages() in same response or as separate endpoint

    4. **GET /api/agent/embed-code/{store_id}** (auth: require_store_owner):
       - Generate embed code using agent_config_service.generate_embed_code(store_id, request.base_url or settings.API_URL)
       - Return EmbedCodeResponse

    Add endpoint tests to `backend/tests/test_agent_config.py` using existing conftest.py patterns (mock DB with monkeypatch). Mock elevenlabs_service calls with AsyncMock.

    IMPORTANT: Do NOT modify the existing PATCH /api/agents/{store_id} endpoint -- the new PUT /api/agent/config/{store_id} is a superset that handles widget settings too. The old endpoint remains for backward compatibility.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_agent_config.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - GET /api/agent/config/{store_id} returns full config from DB
    - PUT /api/agent/config/{store_id} updates ElevenLabs (for agent fields) and DB (for all fields)
    - Enable/disable toggle updates agent_status + settings.enabled
    - Personality preset lookup resolves to system_prompt pushed to ElevenLabs
    - GET /api/voices returns 10 curated voices with preview URLs
    - GET /api/agent/embed-code/{store_id} returns copy-paste HTML
    - All existing tests still pass (no regressions)
  </done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -v` -- all tests pass including new config tests
- `cd backend && python -m pytest tests/test_agent_config.py -v` -- all config-specific tests pass
- No modifications to existing endpoints that could cause regressions
</verification>

<success_criteria>
- 4 new API endpoints operational and tested
- Personality presets return valid system_prompt templates
- Voice list returns 10 curated voices with preview URLs
- Enable/disable toggle propagates to agent_status
- Widget-only changes (color, position) do NOT trigger ElevenLabs API call
- Embed code contains correct store_id and data attributes
- All 74+ existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-configuration/06-01-SUMMARY.md`
</output>
