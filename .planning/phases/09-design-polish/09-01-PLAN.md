---
phase: 09-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/components/DashboardSidebar.tsx
  - frontend/src/app/dashboard/reports/page.tsx
  - frontend/src/app/dashboard/billing/page.tsx
  - frontend/src/app/dashboard/conversations/page.tsx
  - frontend/src/app/dashboard/knowledge-base/page.tsx
  - frontend/src/app/dashboard/agent-config/page.tsx
  - frontend/src/components/ClientDashboard.tsx
autonomous: true
requirements:
  - DSN-02
  - DSN-03
  - DSN-05

must_haves:
  truths:
    - "Dashboard cards use consistent glassmorphism styling across all pages"
    - "Sidebar renders at w-64 on desktop with no layout shift"
    - "Sidebar opens as full-width overlay on mobile with backdrop"
    - "All dashboard pages are responsive at 375px, 768px, 1024px, 1440px"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "glass-card and glass-surface @utility definitions"
      contains: "@utility glass-card"
    - path: "frontend/src/components/DashboardSidebar.tsx"
      provides: "Fixed-width sidebar with w-64"
      contains: "w-64"
  key_links:
    - from: "frontend/src/app/dashboard/reports/page.tsx"
      to: "frontend/src/app/globals.css"
      via: "glass-card utility class"
      pattern: "glass-card"
    - from: "frontend/src/components/DashboardSidebar.tsx"
      to: "frontend/src/app/globals.css"
      via: "sidebar layout using design tokens"
      pattern: "w-64"
---

<objective>
Add glassmorphism design tokens and apply them consistently across all dashboard pages. Fix sidebar sizing for consistent rendering across breakpoints.

Purpose: Establish visual consistency across the dashboard with premium glassmorphism cards and fix the sidebar width to prevent layout shifts.
Output: Updated globals.css with new utilities, all dashboard pages using glass-card, sidebar with fixed w-64 width.
</objective>

<execution_context>
@C:/Users/Nikita/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nikita/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/app/globals.css
@frontend/src/components/DashboardSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glassmorphism utilities and fix sidebar width</name>
  <files>frontend/src/app/globals.css, frontend/src/components/DashboardSidebar.tsx</files>
  <action>
In `frontend/src/app/globals.css`, add two new `@utility` blocks after the existing `glass-panel` utility:

```css
@utility glass-card {
  background: oklch(100% 0 0 / 0.05);
  backdrop-filter: blur(24px);
  border: 1px solid oklch(100% 0 0 / 0.10);
  border-radius: 1rem;
}

@utility glass-surface {
  background: oklch(100% 0 0 / 0.03);
  backdrop-filter: blur(12px);
  border: 1px solid oklch(100% 0 0 / 0.06);
}
```

In `frontend/src/components/DashboardSidebar.tsx`:
1. Change sidebar `<aside>` width from `w-[260px]` to `w-64` (256px, Tailwind standard) for consistent sizing
2. Ensure the mobile overlay uses the same `w-64` (already full-screen via `fixed inset-y-0 left-0`)
3. Verify the `lg:static` + `lg:translate-x-0` pattern is correct for desktop rendering
4. Keep all other sidebar functionality (usage warning, nav items, user card) unchanged

Do NOT change the sidebar's gradient background, border, nav item styling, or any behavior.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>globals.css has glass-card and glass-surface utilities. Sidebar uses w-64 consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Apply glass-card to all dashboard page cards</name>
  <files>frontend/src/app/dashboard/reports/page.tsx, frontend/src/app/dashboard/billing/page.tsx, frontend/src/app/dashboard/conversations/page.tsx, frontend/src/app/dashboard/knowledge-base/page.tsx, frontend/src/app/dashboard/agent-config/page.tsx, frontend/src/components/ClientDashboard.tsx</files>
  <action>
Across ALL dashboard pages, find every card container that uses the pattern `bg-raised rounded-2xl border border-edge` and replace with `glass-card` utility class. Keep any additional classes (padding, margins, etc.).

Specific replacements per file:

1. **reports/page.tsx**: Replace all `bg-raised rounded-2xl border border-edge` card wrappers with `glass-card`. There are ~7 card instances (KPI cards, chart containers, tables). Keep `p-6` and other spacing.

2. **billing/page.tsx**: Replace plan cards, usage meter card, and any info cards. Keep all interactive elements (buttons, links) unchanged.

3. **conversations/page.tsx**: Replace conversation list card wrapper and any detail panels.

4. **knowledge-base/page.tsx**: Replace product list card, sync status card, and form containers.

5. **agent-config/page.tsx**: Replace config section cards (voice picker, widget appearance, personality, etc.).

6. **ClientDashboard.tsx**: Replace overview cards if they use the same pattern.

For each file:
- Search for `bg-raised rounded-2xl border border-edge` or similar solid-bg card patterns
- Replace with `glass-card` (which provides bg, blur, border, and border-radius)
- Keep `p-6`, `p-8`, `mb-*`, `gap-*`, and any grid/flex layout classes
- Do NOT change text colors, icons, interactive elements, or data fetching logic
- Do NOT add hover effects or animations (keep existing ones if any)

Ensure responsive classes (grid-cols-1 md:grid-cols-2, etc.) remain unchanged for mobile responsiveness.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>All dashboard pages use glass-card consistently. Cards have backdrop-blur glassmorphism effect. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes with zero errors
- grep for `glass-card` in dashboard pages returns matches in all 6 files
- grep for `@utility glass-card` in globals.css returns 1 match
- grep for `@utility glass-surface` in globals.css returns 1 match
- DashboardSidebar.tsx contains `w-64` instead of `w-[260px]`
</verification>

<success_criteria>
- glass-card and glass-surface utilities defined in globals.css
- All dashboard card containers use glass-card class
- Sidebar uses w-64 fixed width on desktop, full overlay on mobile
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-polish/09-01-SUMMARY.md`
</output>
