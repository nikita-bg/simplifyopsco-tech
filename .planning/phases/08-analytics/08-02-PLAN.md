---
phase: 08-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/app/dashboard/reports/page.tsx
autonomous: true
requirements:
  - ANL-01
  - ANL-02
  - ANL-03
  - ANL-04
  - ANL-05
  - ANL-06

must_haves:
  truths:
    - "Merchant sees total conversations with up/down trend arrow comparing current vs previous period"
    - "Merchant sees average conversation duration in a readable format"
    - "Merchant sees top intents displayed as readable labels with bar visualization"
    - "Merchant sees peak usage hours as a bar chart (Recharts)"
    - "Merchant sees unanswered questions list with actionable context"
    - "Merchant can click a conversation in the existing Conversations page to see its full transcript"
  artifacts:
    - path: "frontend/src/app/dashboard/reports/page.tsx"
      provides: "Complete analytics dashboard with charts and data"
      min_lines: 150
  key_links:
    - from: "frontend/src/app/dashboard/reports/page.tsx"
      to: "/api/analytics/overview"
      via: "apiFetch in useEffect"
      pattern: "apiFetch.*analytics/overview"
    - from: "frontend/src/app/dashboard/reports/page.tsx"
      to: "/api/analytics/peak-hours"
      via: "apiFetch for Recharts bar chart data"
      pattern: "apiFetch.*analytics/peak-hours"
    - from: "frontend/src/app/dashboard/reports/page.tsx"
      to: "/api/analytics/unanswered"
      via: "apiFetch for unanswered questions list"
      pattern: "apiFetch.*analytics/unanswered"
---

<objective>
Rewrite the Reports/Analytics dashboard page to use the new analytics endpoints, showing trends, peak hours chart, intents, and unanswered questions.

Purpose: Replace the legacy reports page (which calls non-functional endpoints) with a real analytics dashboard powered by the Phase 8 backend endpoints.
Output: Fully functional analytics page with Recharts charts and period selector.
</objective>

<execution_context>
@C:/Users/Nikita/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nikita/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-analytics/08-01-SUMMARY.md

Existing frontend patterns:
- Auth/store: `useStore()` hook provides `storeId`
- API calls: `apiFetch(path)` from `@/lib/api` adds auth token
- Dark theme: bg-canvas (#0f1115), bg-raised (#181b21), text-heading, text-muted, text-faint, border-edge, bg-primary
- Charts: Recharts 3.7.0 already installed (`recharts` in package.json)
- Icons: lucide-react
- Conversations page already exists at `/dashboard/conversations` with list + transcript click-through (ANL-04 satisfied)

API response shapes from Plan 01:
- GET /api/analytics/overview?store_id=X&period=7d -> { total_conversations, prev_total_conversations, avg_duration_seconds, prev_avg_duration_seconds, total_duration_seconds, period }
- GET /api/analytics/intents?store_id=X&period=7d -> { intents: [{ intent, count }], period }
- GET /api/analytics/peak-hours?store_id=X&period=7d -> { hours: [{ hour: 0-23, count }], period }
- GET /api/analytics/unanswered?store_id=X&period=7d -> { questions: [{ intent, summary, date }], total, period }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite reports page as analytics dashboard</name>
  <files>frontend/src/app/dashboard/reports/page.tsx</files>
  <action>
Complete rewrite of the Reports page. Keep the same route `/dashboard/reports` (sidebar already links here).

**Layout structure (top to bottom):**

1. **Header row:** Title "Analytics" + period selector (3 buttons: 7D / 30D / 90D, styled as pill toggle group, default 7D)

2. **KPI cards row** (grid sm:grid-cols-2 lg:grid-cols-4 gap-5):
   - **Total Conversations**: Big number + trend arrow (green ArrowUpRight if current > prev, red ArrowDownRight if less, gray Minus if equal). Show percentage change: `Math.round(((current - prev) / (prev || 1)) * 100)%`. Use MessageSquare icon.
   - **Avg Duration**: Format avg_duration_seconds as "Xm Ys" (e.g., "2m 34s"). Show trend arrow same logic with prev_avg_duration_seconds. Use Clock icon.
   - **Total Duration**: Format total_duration_seconds as hours+minutes (e.g., "4h 12m"). Use Timer icon.
   - **Unanswered Questions**: Show total count from unanswered endpoint. Use HelpCircle icon, orange/warning color.

3. **Two-column grid (lg:grid-cols-2 gap-6):**

   **Left: Peak Hours chart**
   - Title "Peak Usage Hours" with BarChart3 icon
   - Recharts BarChart with: `<ResponsiveContainer width="100%" height={280}>`, `<BarChart data={hours}>`, `<XAxis dataKey="hour" tick={{ fill: '#6b7280', fontSize: 12 }} tickFormatter={h => `${h}:00`} />`, `<YAxis tick={{ fill: '#6b7280', fontSize: 12 }} />`, `<Bar dataKey="count" fill="#256af4" radius={[4, 4, 0, 0]} />`
   - No grid, dark theme compatible. Use `<Tooltip contentStyle={{ backgroundColor: '#181b21', border: '1px solid #2a2d35', borderRadius: 8 }} labelFormatter={h => `${h}:00 - ${h+1}:00`} />`

   **Right: Top Intents**
   - Title "Top Customer Intents" with BarChart3 icon
   - Horizontal bar visualization (same pattern as existing reports page — CSS bars, not Recharts). Show intent name + count. Max 10 intents. Calculate percentage relative to highest count for bar width.

4. **Unanswered Questions section** (full width):
   - Title "Unanswered Questions" with HelpCircle icon + total count badge
   - If no questions: "No unanswered questions — your agent is handling everything!" with a green check icon
   - If questions exist: List with each item showing intent (bold), summary text (truncated), and date (relative like "2 days ago"). Style as rows in a card, separated by border-edge dividers.
   - Limit display to 20 items. If total > 20, show "Showing 20 of {total}" footer.

**State management:**
- `period` state (default "7d")
- `overview`, `intents`, `peakHours`, `unanswered` state objects
- `loading` boolean
- Single `useEffect` that fires on `[storeId, period]` changes
- Fetch all 4 endpoints in parallel with `Promise.all`

**Imports:**
- `{ useState, useEffect }` from react
- `{ BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer }` from recharts
- Icons from lucide-react: `Loader2, MessageSquare, Clock, Timer, HelpCircle, BarChart3, ArrowUpRight, ArrowDownRight, Minus, CheckCircle`
- `useStore` from `@/lib/store-context`
- `apiFetch` from `@/lib/api`

**Styling:** Follow existing dark theme patterns. Cards use `bg-raised rounded-2xl border border-edge p-6`. Inner elements use `bg-canvas`. Consistent with billing, config, and conversations pages.

Helper function `formatDuration(seconds: number): string` that returns "Xm Ys" format.
Helper function `timeAgo(dateStr: string): string` for relative dates (reuse pattern from dashboard if exists, otherwise: today/yesterday/N days ago).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Analytics page renders with period selector, 4 KPI cards with trend arrows, Recharts peak hours bar chart, top intents bars, and unanswered questions list. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit
# Manual: visit /dashboard/reports to verify charts render
</verification>

<success_criteria>
- Reports page loads without errors and calls all 4 analytics endpoints
- Period selector switches between 7D/30D/90D and re-fetches data
- KPI cards show trend arrows with percentage change
- Peak hours renders as a Recharts BarChart with 24 bars
- Top intents display as horizontal bars with labels and counts
- Unanswered questions show as a scrollable list
- Conversations page (already built, ANL-04) provides transcript click-through
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics/08-02-SUMMARY.md`
</output>
